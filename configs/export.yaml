# Placeholder export config for raw sim dump
dataset: handover-sim
target_frame: world
required_fields:
  - episode_id
  - t
  - dt
  - hand_pose
  - object_pose
  - gripper_pose
  - gripper_width
  - contact_flag
  - visibility
notes: "Use this as the contract for export_raw scripts."
