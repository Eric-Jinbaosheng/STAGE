from linguistic_blindness.models.base import UnavailableModel


def build_model():
    return UnavailableModel("prompted_vlm_schema", "Prompted VLM wrapper is intentionally stubbed until raw model generation is run on the counterfactual benchmark.")
